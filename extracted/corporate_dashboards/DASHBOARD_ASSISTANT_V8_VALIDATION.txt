Dashboard Assistant V8 validation
==================================

- All C# files passed lexical brace validation.
- Version 217 direct route executes before generic dataset scoring.
- Payment Value is the deterministic default for “how much / paid”.
- Credit Card resolves to payment_type = Credit Card through semantic aliases.
- Since April 2026 uses the inclusive since-range parser.
- No grouping forces metric + narrative and suppresses graph rendering.
- Executive execution is hard-bound to suite/version 217.
- Context fallback recognizes exact executive template and exact Customer Payments title.
- Client and server both require build assistant-v8-direct-v217-contract.
- Visual Studio Project profile uses unique ports 49582/49583 to avoid an older clone.
- No bin, obj, .vs, DLL, or PDB artifacts are included.

Exact request contract:
  how much was paid by credit card since april 2026
  -> payment_value
  -> payment_type = Credit Card
  -> from 2026-04-01 inclusive through current-month start exclusive
  -> Sum
  -> no dimensions
  -> metric + narrative; no graph
